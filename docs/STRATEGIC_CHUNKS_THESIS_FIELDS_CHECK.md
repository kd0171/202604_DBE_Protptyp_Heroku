# Strategic Chunks thesis field alignment

Updated the Prompt Test Strategic Evidence Chunk output target to explicitly include the thesis-aligned fields used in the written explanation:

- `company` / `company_name`
- `category`
- `topic`
- `strategic_signal`
- `time_horizon` with `short-term`, `mid-term`, `long-term`, or `unclear`
- `document_type`
- `year` / `reporting_year`
- `content`

The downstream prompts were left unchanged.
