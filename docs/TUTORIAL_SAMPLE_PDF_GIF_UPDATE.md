# Tutorial Sample PDF GIF Update

The first uploaded tutorial GIF has been added to the Data Engineering tutorial.

## Added file

- `assets/tutorial/download_sample_pdf.gif`

## UI location

The GIF is displayed in tutorial step 2, now titled **Download the Sample PDF**. It shows the user clicking the `Sample PDF` button to download the ABF Annual Report 2025 sample document.

## Rationale

This step clarifies that the prototype uses one supported sample PDF and that users should download it first, then upload it into the `Upload PDF` box to trigger the simulated pipeline.
